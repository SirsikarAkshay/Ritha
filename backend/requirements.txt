# ─────────────────────────────────────────────────────────────────────────────
# Ritha — Full development dependencies
#
# = requirements-ci.txt (the lean/standard set) + the heavy ML *training* stack
# below. CI installs requirements-ci.txt only; install THIS file locally when you
# need model training/inference (ml/train.py, ml/inference.py).
#
# Add normal runtime/test deps to requirements-ci.txt, not here, so both dev and
# CI pick them up. For production, see requirements_prod.txt.
# ─────────────────────────────────────────────────────────────────────────────

-r requirements-ci.txt

# ML — fashion recommendation training/inference stack (heavy; CI-excluded
# because no test imports it. numpy/pandas/joblib live in requirements-ci.txt).
torch>=2.0
torchvision>=0.15
scikit-learn>=1.9.0
