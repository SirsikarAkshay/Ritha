# ─────────────────────────────────────────────────────────────────────────────
# Ritha — CI / lean dependency set
#
# This is the standard development set MINUS the heavy ML *training* stack
# (torch / torchvision / scikit-learn). No test imports those at module level —
# numpy is only imported lazily inside functions — so CI installs neither, which
# cuts hundreds of MB (torch + CUDA + scipy) off every run.
#
# requirements.txt = this file + the ML training stack (see that file).
# Add normal runtime/test deps HERE so both dev and CI pick them up.
# ─────────────────────────────────────────────────────────────────────────────

# Core
django>=5.2,<6.0
djangorestframework>=3.17.1,<4.0
djangorestframework-simplejwt>=5.3,<6.0
django-cors-headers>=4.3,<5.0
# Pinned exactly: drf-spectacular is a code-generator, and its output (openapi.yaml)
# is committed and gated by the CI schema-drift check. A floating version means CI
# can resolve a newer release than the one used to generate the committed schema,
# producing spurious drift. Bump deliberately + regenerate via `make codegen`.
drf-spectacular==0.30.0

# Environment
python-dotenv>=1.2.2,<2.0

# Database driver — CI runs the suite against a real Postgres service container
# (prod parity). The heavy torch/sklearn stack stays out, but the DB driver is
# lightweight and required whenever DATABASE_URL points at Postgres.
psycopg2-binary>=2.9,<3.0

# Images
Pillow>=10.0,<12.0

# HTTP (weather service)
requests>=2.31,<3.0

# Calendar integrations
google-auth-oauthlib>=1.2,<2.0
google-auth-httplib2>=0.2,<1.0
google-api-python-client>=2.130,<3.0
msal>=1.28,<2.0
caldav>=1.3,<2.0
icalendar>=5.0,<6.0
cryptography>=42.0,<44.0
python-dateutil>=2.9.0.post0,<3.0
pytz>=2026.2

# AI — uncomment to activate live agents
mistralai>=1.0,<2.0

# Firebase (push notifications)
firebase-admin>=6.5,<7.0

# ML — lightweight pieces used on lazy paths (the heavy torch/sklearn stack
# lives in requirements.txt, not here).
pandas>=3.0.3
numpy>=1.24
joblib>=1.3

# ── Lint / format ─────────────────────────────────────────────────────────────
ruff>=0.6,<1.0

# ── Dev & Testing ─────────────────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-django>=4.8,<5.0
pytest-cov>=7.1.0,<8.0
factory-boy>=3.3,<4.0

# Task queue (dev)
celery>=5.3,<6.0
django-celery-beat>=2.7,<3.0

# WebSockets / real-time (Django Channels)
channels>=4.0,<5.0
channels-redis>=4.2,<5.0
daphne>=4.1,<5.0
